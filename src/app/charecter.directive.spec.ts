import { CharecterDirective } from './charecter.directive';

describe('CharecterDirective', () => {
  it('should create an instance', () => {
    const directive = new CharecterDirective();
    expect(directive).toBeTruthy();
  });
});
